import BackendUnavailable from '@/components/common/BackendUnavailable';

export default function AssistantContactPage() {
  return (
    <BackendUnavailable
      title="Liên hệ nội bộ"
      description="Backend hiện chưa có API liên hệ nội bộ cho phụ xe."
    />
  );
}
