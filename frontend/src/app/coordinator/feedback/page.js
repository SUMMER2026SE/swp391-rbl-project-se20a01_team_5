import BackendUnavailable from '@/components/common/BackendUnavailable';

export default function CoordinatorFeedbackPage() {
  return (
    <BackendUnavailable
      title="Phản hồi & thông báo"
      description="Backend hiện chưa có API phản hồi, xử lý khiếu nại hoặc gửi thông báo từ điều phối."
    />
  );
}
